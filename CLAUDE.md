# テニスフィーバー プロジェクト

## 概要
子供向けのマリオテニスフィーバー風ブラウザテニスゲーム。
iPadのホーム画面にアイコンとして置いて、子供がタップするだけで遊べるようにしたい。

## ファイル構成
```
tennis-fever/
├── index.html     ← ゲーム本体
├── icon.png       ← テニスボールアイコン（192x192px）
└── CLAUDE.md      ← このファイル
```

## ゲームの仕様

### モード（3種類）
- **1人用**：あなた(選択) + CPU(選択) vs CPU(選択) + CPU(選択)
- **2人用A**：P1(選択) + P2(選択) vs CPU + CPU
- **2人用B**：P1(選択) + CPU(選択) vs P2(選択) + CPU(選択)
- 全モードでキャラ選択あり

### キャラクター（4体）
| キャラ | 特殊能力 |
|--------|----------|
| マリオ🔴 | 打った球を受けた相手1人を後ろに吹き飛ばす |
| ルイージ🟢 | 移動速度1.2倍 |
| ロゼッタ⭐ | 打った球が0.8倍に減速・コート中央方向に曲がる |
| 骨クッパ💀 | 打つたびに火の玉を発射。当たると1秒スタン |

### スコアルール
- 1〜7点制（7点先取でゲーム獲得）
- 6-6以降はデュース→2点差でゲーム獲得
- 3ゲーム先取でマッチ勝利

### ダブルスの配置
- 各チーム前衛・後衛の2人がコートに立つ
- **サポートCPU**（人間の相方）はプレイヤーがいない側をカバーする守備的な動き
- **敵CPU**は積極的にボールを追う

### 操作
- **P1**：指でなぞって移動 / 2本指タップでフィーバー / Fキー
- **P2**：矢印キー / 0キーでフィーバー

### ゲームシステム
- **フィーバーショット**：ラリーでゲージを溜めて発動（炎・氷・雷）
- **カウンター**：フィーバーショットを打ち返すと奪い返せる
- **HPシステム**：フィーバー・火の玉でHP減少
- **サーブカウントダウン**：3・2・1・サーブ！の後に打ち出し
- **ボールの跳ね**：重力物理シミュレーション（bz/bvz変数）
- **マリオのmarioBlastフラグ**：打った球に印をつけて受けた相手を飛ばす

## 技術的なポイント

### Safari（iPhone/iPad）対応の鉄則
- `ctx.roundRect()` は未対応 → 自前の `rrect()` / `rrectTop()` 関数を使う
- `const` / `let` は使わず全て `var`
- `**`（べき乗演算子）は使わず `v*v` で記述
- `{ alpha: false }` オプションは使わない
- アロー関数 `=>` は使わず `function(){}` を使う
- テンプレートリテラル（バッククォート）は使わない

### Canvas・描画
- `resizeCanvas()` で画面サイズに合わせて動的リサイズ（正方形・1:1比率）
- `devicePixelRatio` 対応でRetina表示対応
- 固定タイムステップ（60fps）でカクカク防止
- サブステップ分割（4分割）でトンネリング防止

### キャラクター描画
- 全キャラCanvasで手描き（Safari互換のrrect使用）
- マリオ・ルイージ：帽子・オーバーオール・ヒゲの本格的なデザイン
- 骨クッパ：骨格ボディ・赤髪・甲羅・赤い目
- ロゼッタ：ターコイズウェア・金髪・銀王冠・青緑の目

### ボールの跳ねアニメーション
- `bz`（高さ）・`bvz`（垂直速度）で重力物理シミュレーション
- 重力=0.15、初速=7.5（速度に応じて高く跳ねる）
- 影がbzに連動して小さく・薄くなる

### PWA対応
- `apple-mobile-web-app-capable` などのmetaタグ設定済み
- `apple-touch-icon` に icon.png を指定
- インラインでWebApp Manifestを生成
- Service Worker登録済み（オフライン対応）
- **icon.pngはHTMLと同じフォルダに必須**

## デプロイ方法

### Netlify Drop（一番簡単）
1. `app.netlify.com/drop` にアクセス
2. `index.html` と `icon.png` をドラッグ＆ドロップ
3. URLが発行される（アカウント不要・無料）

### GitHub Pages
1. `tennis-fever` リポジトリを作成（Public）
2. `index.html` と `icon.png` をアップロード
3. Settings → Pages → Branch を main に設定
4. `https://ユーザー名.github.io/tennis-fever/` でアクセス

## iPadホーム画面への追加
1. SafariでURLを開く
2. 共有ボタン（□↑）→「ホーム画面に追加」
3. テニスボールアイコンでホーム画面に追加される

## 今後やりたいこと（未実装）
- BGM・効果音
- スコアランキング・ハイスコア保存
- キャラごとの特殊モーション
- コースによってコートの色が変わる
